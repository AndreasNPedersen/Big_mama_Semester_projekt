@page "{id}"
@model MadOrderingssystem.Pages.MenuPage.DeleteAccessoryModel
@{
    ViewData["Title"] = "Accessory Delete";
    @using MadOrderingssystem.Models;
}

@if (Model.CustomerSession != null)
{
    @if (Model.CustomerSession.Role == Models.Roles.Management)
    {
        <div class="container">

            <h1 class="text-center"> Er du sikker på at slette Tilbøret?</h1>

            <form class="form-group" method="post">
                <label class="form-text">Tilbehøret</label>
                <input type="text" readonly class="form-control" asp-for="@Model.Accessory.ProductName" />
                <span asp-validation-for="@Model.Accessory.ProductName" class="text-danger"></span>

                <label class="form-text">Pris</label>
                <input type="text" readonly class="form-control" asp-for="@Model.Accessory.Price" />
                <span asp-validation-for="@Model.Accessory.Price" class="text-danger"></span>

                <label class="form-text">Id</label>
                <input type="text" readonly class="form-control" asp-for="@Model.Accessory.Id" />
                <span asp-validation-for="@Model.Accessory.Id" class="text-danger"></span>

                <div style="padding:1rem;"></div>
                <input type="submit" class="btn btn-danger float-right" value="Slet Tilbehøret" placeholder="Delete" style="padding:1rem;" />
            </form>

            <div>
                <a asp-page="MenuTable">Tilbage til Menu listen</a>
            </div>
        </div>
    }
}
else
{
    <h1 class="text-center" style="font-family:'Times New Roman', Times, serif"> Du er ikke logget ind</h1>
    <div>
        <a asp-page="/Login/log_in">Login med din bruger</a>
    </div>
}
